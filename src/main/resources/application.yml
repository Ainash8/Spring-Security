server:
  port: 9000

spring:
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/schooldb
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true
    show-sql: true
